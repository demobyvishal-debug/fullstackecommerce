import Link from 'next/link';import {rupees} from '@/lib/http';export default function ProductCard({product}:any){return <Link className="card" href={`/product/${product.slug}`}><img src={product.imageUrl||'/placeholder.svg'} alt={product.name}/><div className="card-content"><span className="muted">{product.category.name}</span><h3>{product.name}</h3><p className="price">{product.variants[0]?rupees(product.variants[0].price):'Unavailable'}</p></div></Link>}
