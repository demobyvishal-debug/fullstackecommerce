import {adminUser} from '@/lib/auth';import {db} from '@/lib/db';import {error} from '@/lib/http';import {z} from 'zod';const schema=z.object({name:z.string().min(2),slug:z.string().regex(/^[a-z0-9-]+$/)});export async function POST(req:Request){if(!await adminUser())return error('Forbidden',403);const p=schema.safeParse(await req.json());if(!p.success)return error('Invalid category');try{return Response.json(await db.category.create({data:p.data}),{status:201});}catch{return error('Category already exists',409);}}
