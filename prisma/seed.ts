import {PrismaClient} from '@prisma/client';import bcrypt from 'bcryptjs';const db=new PrismaClient();async function main(){const category=await db.category.upsert({where:{slug:'essentials'},update:{},create:{name:'Essentials',slug:'essentials'}});await db.product.upsert({where:{slug:'canvas-tote'},update:{},create:{name:'Canvas Tote',slug:'canvas-tote',description:'A durable everyday canvas bag.',categoryId:category.id,featured:true,variants:{create:{sku:'TOTE-001',label:'Natural',price:79900,stock:20}}}});if(process.env.SEED_ADMIN_EMAIL&&process.env.SEED_ADMIN_PASSWORD){if(process.env.SEED_ADMIN_PASSWORD.length<12)throw Error('Admin password must be at least 12 characters');await db.user.upsert({where:{email:process.env.SEED_ADMIN_EMAIL},update:{},create:{email:process.env.SEED_ADMIN_EMAIL,name:'Store Owner',passwordHash:await bcrypt.hash(process.env.SEED_ADMIN_PASSWORD,12),role:'SUPER_ADMIN'}});}}main().finally(()=>db.$disconnect());
